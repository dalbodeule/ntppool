version: '3.9'

services:
  ntpd:
    image: nicholasdille/openntpd:latest
    container_name: ntpd
    privileged: true
    restart: unless-stopped
    ports:
      - "123:123/udp"
    volumes:
      - ./ntpd.conf:/etc/ntpd.conf:ro
      - ./ntpd.drift:/var/db/ntpd.drivt:rw
    cap_add:
      - SYS_TIME
    environment:
      - TZ=Asia/Seoul